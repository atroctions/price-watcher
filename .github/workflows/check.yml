name: Price Watcher

on:
  schedule:
    - cron: "*/1 * * * *"   # 30 dakikada bir çalışır
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Repo'yu çek
        uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Gerekli kütüphaneler
        run: pip install requests beautifulsoup4

      - name: Fiyat kontrol scripti
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python check_price.py
